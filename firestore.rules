rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // ⚠️ WARNING: OPEN RULES - ALLOW ALL ACCESS
    // This is for development/testing only
    // DO NOT use in production!
    
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
